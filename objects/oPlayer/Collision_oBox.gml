/// @description Insert description here
// You can write your code in this editor

//horizontal_movement = true;



/*if (x <= other.x - other.sprite_width*0.2)
{
	interaction_radius = (sprite_width / 2);
}

if !place_meeting(x,y-2,oPlayer)
 { 
     if place_meeting(x+2,y,oPlayer) keyboard_check(vk_anykey) 
 }
//	
if !place_meeting(x-2,y,oBox) x-=2;
	
	
    if place_meeting(x-2,y,oPlayer) keyboard_check(vk_anykey) 
	 {
		  if !place_meeting(x+2,y,oBox) x+=2; 
	 } 